<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tsxygfy.seckill.mapper.OrderMapper" >

    <insert id="createOrder" parameterType="cn.tsxygfy.seckill.entity.Order" keyProperty="id" useGeneratedKeys="true">
        insert into orders values ( #{id},#{sid},#{name}, #{createTime})
    </insert>
</mapper>
